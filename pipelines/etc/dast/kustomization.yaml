apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - pairs:
      deployment: kuadrant-qe-pipeline
      app: etc-dast

components:
  - ../../../tasks/login/
  - ../../../tasks/etc/

resources:
  - pipeline.yaml
